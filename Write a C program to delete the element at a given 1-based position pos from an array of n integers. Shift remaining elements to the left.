#include <stdio.h>
int main() {
int n, pos;
int arr[101];

scanf("%d",&n);

for(int i=0;i<n;i++){
    scanf("%d",&arr[i]);
}

scanf("%d",&pos);
for(int i=pos-1;i<n-1;i++){
    arr[i] = arr[i+i];
    
}

for(int i=0;i<n-1;i++){
    printf("%d",arr[i]);
    if(i<n-2) printf(" ");
}
printf("\n");
return 0;
}
